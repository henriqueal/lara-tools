#include "fldry.c"
int main( argc, argv)
int argc;
char **argv;
{
  printf("calculate floating dhrystones using doubles size %d\n",
         sizeof(extended));
	//fflush( stdout);
	Proc0();
        return 0;
}
